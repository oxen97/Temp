/** User-facing additions are centralized so they can be translated later. */
export const interactionPanelCopy = {
  strandTipLength: {
    label: "Tip length",
    ariaLabel: "Strand tip length",
    help: "0 stretches the whole asset. A positive length preserves the free tip's shape while the body stretches.",
  },
} as const;
